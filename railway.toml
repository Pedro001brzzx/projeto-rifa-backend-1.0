[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python scripts/init_db.py && gunicorn wsgi:app --workers 2 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
FLASK_ENV = "production"
PYTHON_VERSION = "3.11"
